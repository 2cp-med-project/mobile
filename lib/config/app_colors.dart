// config/app_colors.dart
// All app colors — never use raw hex codes anywhere else

import 'package:flutter/material.dart';

class AppColors {
  static const primary    = Color(0xFF1FAF87);  // main green
  static const primaryLight = Color(0xFFE8FAF5); // light green for bubbles
  static const error      = Color(0xFFE53935);  // red for errors
  static const textGrey   = Color(0xFFAAAAAA);  // grey for hints/subtitles
  static const textDark   = Color(0xFF333333);  // dark for input text
  static const border     = Color(0xFFCCCCCC);  // grey border
}